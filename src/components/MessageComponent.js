import React , {useState, useEffect} from 'react';
import * as firebase from 'firebase';

export default function Message(props){
    return(
        <div>
            Message Component says Hi ! to you
        </div>
    );
}